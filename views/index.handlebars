<h1>It's your cheat day. Eat dat Burger!</h1>
<p>Choose a burger or make a burger, then devour it!</p>

<ul>
  {{#each burger}}
  	<li>
  		<p><span class="label">ID:</span> {{this.id}}</p>
      <p><span class="label">Burger:</span> {{this.burger_name}}</p>

      <button data-id="{{this.id}}" class="delburger">Delete</button>

      <a href="/{{this.id}}">Update This Burger</a>
  	</li>
  {{/each}}
</ul>

<h2>Add New Burger</h2>

<form class="create-form">

  <div class="form-group">
    <label for="burgeradd">New Burger:</label>
    <input type="text" id="burgeradd" name="NewBurger">
  </div>

  <button type="submit">Add Dat Burger</button>
</form>
